import { Bangers, Red_Hat_Display } from 'next/font/google';

export const bangers = Bangers({
  weight: '400',
  subsets: ['latin'],
  display: 'swap',
});

export const redHatDisplay = Red_Hat_Display({
  subsets: ['latin'],
  display: 'swap',
});